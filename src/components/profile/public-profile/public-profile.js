import React from "react";

const PublicProfile = ({id}) => {
    return (
        <div>
            Get info for user with id:{id} and populate page
        </div>
    )
}

export default PublicProfile;